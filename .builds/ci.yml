image: archlinux
packages:
  - rust
  - node
sources:
  - https://git.sr.ht/~garritfra/antimony
tasks:
  - build: |
      cargo build
      cargo test
      cargo clippy --all-targets --all-features -- -D warnings
